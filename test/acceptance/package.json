{
  "name": "acceptance-tests",
  "description": "Acceptance tests for the application",
  "private": true,
  "scripts": {
    "test": "TIMEOUT=60000 PORT=5173 playwright test",
    "test:ci": "TIMEOUT=300000 NODE_ENV=production playwright test",
    "app:dev": "cd ../.. && PORT=5173 pnpm run dev",
    "app:build": "cd ../.. && pnpm build",
    "app:start": "cd ../.. && PORT=5173 pnpm preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1"
  }
}
